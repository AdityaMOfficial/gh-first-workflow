name: Basic Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
      - run: echo "Hello boy"
      - name : Print Goodbye
      - run: echo "Done, Goodbye"
